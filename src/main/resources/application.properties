# ==============================
# Application Configuration
# ==============================
spring.application.name=where-we-go
# ==============================
# DataSource Configuration
# ==============================
spring.datasource.url=jdbc:mysql://192.168.1.20:3306/wherewego
spring.datasource.username=
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# ==============================
# JPA Configuration
# ==============================
# Tables are recreated on every run. In production, change to 'update' or 'validate'.
spring.jpa.hibernate.ddl-auto=create
spring.jpa.open-in-view=false
# Hibernate Logging Configuration
spring.jpa.properties.hibernate.show_sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
# ==============================
# Redis Configuration
# ==============================
# Currently using local Redis for development.
# In production, Redis host and authentication settings are required.
# spring.data.redis.host=${REDIS_HOST}
# spring.data.redis.port=${REDIS_PORT}
# spring.data.redis.password=${REDIS_PASSWORD}
spring.data.redis.host=localhost
spring.data.redis.port=6379
# ==============================
# JWT Configuration
# ==============================
jwt.secret.key=550b2ddb449e35dc903716f37f095c9be3334c740d9214311bd543501036ad86
# ==============================
# Server Configuration
# ==============================
server.port=8080
server.error.include-message=always
# ==============================
# External API Configuration
# ==============================
# Kakao API
kakao.api.key=121212
# Google Maps API
google.api.key=23232323
# ==============================
# Logging Configuration
# ==============================
# Global log level
logging.level.root=INFO
# Application logs with detailed debugging
logging.level.com.example.wherewego=DEBUG
# Show SQL queries (without parameters)
logging.level.org.hibernate.SQL=DEBUG
# Uncomment to show SQL parameter values (verbose)
# logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
# Spring framework logs - warnings and above only
logging.level.org.springframework=WARN
logging.level.org.springframework.web=INFO
